# 代码体检报告：死代码 / 设计不合理

> 项目：`deepseek-harness-desktop`（Tauri 2 / macOS 薄启动器）
> 范围：Rust 后端（`src-tauri/src/*`）+ 注入脚本（`inject_js/*` 的 `.rs` 与 `js/`）+ 启动器 UI（`ui/*`）
> 方法：`cargo check --lib`（零警告）+ `cargo clippy -D warnings`（CI 已启用）+ grep 可达性分析 + Tauri capability / `invoke_handler` 交叉核对 + 人工逐模块阅读
> 结论倾向：**本项目几乎没有传统"死代码"**；真正值得修的是**一处权限接线断裂（bug）**、若干**调试遗留**与**文档漂移**。

---

## 0. 结论速览

| 类别 | 结论 |
| --- | --- |
| 死代码（Rust 私有函数） | **无**。编译器零警告（`cargo check` / `clippy -D warnings` 已覆盖） |
| 死代码（Rust `pub` 导出项） | **无**。所有 `lib.rs` 再导出的项（含 `parse_status_code`、`sessions_root` 等）均有真实调用点 |
| 死代码（注入 JS 片段） | **无**。`js/` 下 19 个片段全部被某个 `include_str!` 引用，且都被 `mod.rs::js_module()` 拼入包 |
| 死代码（启动器 UI） | **无**。`ui/app.js` 内 `render`/`escapeHtml`/`getStatus`/`save`/`restart`/`poll` 全部被引用 |
| 真正 bug | **P0：`read_file_base64` 漏加 capability 授权 → 图片预览运行时被拒** |
| 调试遗留 | P1：`/tmp/dsh_copy.log`、`/tmp/dsh_reveal.log` 无条件写入；P2：`file_tree.js` 残留 `console.log` |
| 设计不合理 | P2：`check-js.sh` 未接 CI（JS 守门实际失效）；P2：`inject_js` 片段清单在 3 处手工同步；P2：脆弱的花括号计数单测 |
| 文档漂移 | P1：README 只描述了"归档按钮"，未记录已发布的 right_sidebar / file_link 功能 |

> 注：git 历史里"移除 Web Share（局域网分享）"的清理是干净的——仅在依赖编译文档 `target/doc/*` 出现 `share` 字样，项目源码无残留。

---

## 1. 已直接修复的项（按你的授权）

### P0 · `read_file_base64` 权限接线断裂（核心 bug）

**现象**：右侧栏图片预览功能在运行时静默失效。

**根因**：`read_file_base64` 这一命令在三个地方都齐了，唯独漏了第四处——
- ✅ 已注册：`src-tauri/src/main.rs:100`（`generate_handler!`）
- ✅ 已生成权限：`src-tauri/permissions/autogenerated/read_file_base64.toml`（`allow-read-file-base64` 存在）
- ✅ JS 已调用：`src-tauri/src/inject_js/js/right_sidebar/modal.js:51`（`tauriInvoke('read_file_base64', ...)`）
- ❌ **漏授权**：`src-tauri/capabilities/dsh-page.json` 的 `permissions` 数组里没有 `allow-read-file-base64`

由于该命令由注入到 `http://127.0.0.1:*` 的远程页调用，而 capability 未授予，Tauri 在运行时以"权限不足"拒绝 → 文本预览（`read_file_content`，已授权）正常，图片预览（`read_file_base64`）失败。这是比死代码更严重的问题，且 `cargo check`/`clippy` 不会报（构建期只校验权限标识符存在，不校验 capability 是否授予）。

**修复**：在 `capabilities/dsh-page.json` 的 `permissions` 中补 `"allow-read-file-base64",`，并同步更新该 capability 的 `description`（原描述称"归档命令是唯一远程页可调用命令"，与现状不符）。

### P1 · `commands.rs` 两处 `/tmp` 调试日志（调试遗留）

`copy_to_clipboard`（原 `commands.rs:543-546`）与 `reveal_in_finder`（原 `commands.rs:565-568`）在每次调用后用 `let _ = std::fs::write("/tmp/dsh_copy.log" / "dsh_reveal.log", ...)` 无条件写调试日志，且用 `let _ =` 吞掉错误。这是开发期遗留，会：
- 每次复制/打开访达都落盘 `/tmp`，无开关、无清理；
- 在沙盒/无写权环境静默失败（被 `_` 吞掉），掩盖真实问题。

**修复**：删除这两段 `std::fs::write` 调试日志。

### P2 · `file_tree.js` 残留 `console.log`

`src-tauri/src/inject_js/js/right_sidebar/file_tree.js:344` 在 `reveal_in_finder` 调用前有一行 `console.log('[dsh] reveal_in_finder', path, isDir);` 调试输出，已删除。（同文件 `highlight.js` 里的 `debugger` 是 JS 关键字高亮词表字符串的一部分，非真实调用，保留无误。）

### P2 · `check-js.sh` 未接入 CI（设计/流程漏洞）

`scripts/check-js.sh` 用 `node --check` 对每个 JS 片段 + 完整 IIFE 拼装产物做语法守门，但 `.github/workflows/ci.yml` 只跑了 `cargo fmt / clippy / test / check`，**从未调用该脚本**——意味着这套 JS 守门实际上不生效，注入脚本语法错误只能靠用户本地跑或运行时才暴露。

**修复**：在 `ci.yml` 增加 `JS injection syntax gate` 步骤（`bash scripts/check-js.sh`）。macOS runner 自带 node，脚本内部也做了 `command -v node` 兜底校验。

### P2 · 移除 `inject_js/mod.rs` 脆弱的花括号计数单测

原 `braces_are_balanced_across_all_fragments` 测试统计整包 `{`/`}` 数量是否相等，是语法正确性的**弱代理**：字符串字面量里一旦出现未配对花括号就会误报，且它只数数量不校验位置。真正的语法校验已由 `check-js.sh` 的 `node --check`（现已接入 CI）承担，更权威。

**修复**：删除该测试，保留 `assembled_bundle_is_an_iife`（校验 IIFE 骨架与 `initArchive()`/`initRightSidebar()` 入口存在，仍有结构价值）。

### P1 · 文档漂移（以代码为准）

README 第 4 点只写了"向页面注入「归档所有对话」侧边栏按钮"，与代码实际注入的**三类脚本**（归档按钮 + 右侧栏 Git/文件树/预览 + 文件链接拦截）不符。已按代码补全 README 第 4 点，并同步更新 `capabilities/dsh-page.json` 描述。

---

## 2. 仅建议、未改的项（按你的授权保持范围）

> 以下为"设计不合理"层面的改进建议，本次未动手；如需我执行可再确认。

### P2 · `inject_js` 片段清单在 3 处手工同步（易脱节）

同一份 JS 片段清单/顺序被硬编码在：
1. `src-tauri/src/inject_js/archive/mod.rs` 的 `push_str` 顺序；
2. `src-tauri/src/inject_js/right_sidebar/mod.rs` 的 `push_str` 顺序；
3. `scripts/check-js.sh` 的 `ARCHIVE=(...)` / `SIDEBAR=(...)` 数组（其注释已明说"必须与 mod.rs 顺序一致"）。

三处任一改了另两处忘了改，就会出现"编译通过但注入内容错位/校验失准"。建议：用脚本在构建期自动枚举 `js/` 目录生成拼装顺序，或在 `check-js.sh` 里 `diff` 实际 `include_str!` 列表与脚本列表，把"手工同步"变成"自动校验"。

### P3 · `get_git_diff` 连发 3 次 `git` 子进程

`commands.rs::get_git_diff` 分别执行 `git diff --stat`、`git diff --name-status`、`git diff`（3 次 spawn）。可合并为 1 次 `git diff` + 1 次 `git diff --stat`，文件状态（M/A/D/R）由完整 diff 的 `diff --git` 头解析，减少进程开销与潜在不一致。（纯效率/简化，非 bug。）

### P3 · `ArchiveOpResponse.restart_triggered` 恒为 `false`

`commands.rs` 中该字段对外（注入面板）暴露但永远为 `false`，并配大段注释解释"为何不再强制重启 dsh"。作为 IPC 返回契约略冗余——前端本可忽略该字段。建议保留字段但注明其为保留位，或后续版本直接移除以简化契约。

### P3 · `encode_base64` 手写实现

`commands.rs::encode_base64` 手写 base64（注释称"为避免引入额外依赖"）。功能正确，但与"不引入依赖"的取向一致，属于合理取舍；仅作为设计备注提出，不建议为它单独加 crate。

---

## 3. 验证记录

| 检查 | 命令 | 结果 |
| --- | --- | --- |
| Rust 编译 | `cargo check --lib` | 退出码 0，零警告 |
| JS 语法守门 | `bash scripts/check-js.sh` | `OK: 19 个片段 + 完整 bundle 语法全部通过` |
| 调试日志残留 | `grep "/tmp/dsh_copy.log" "/tmp/dsh_reveal.log"` | 无残留 |
| console.log 残留 | `grep "console.log" inject_js/**/*.js` | 无残留 |
| capability JSON | `python3 -m json.tool` | 合法 |

---

## 4. 改动文件清单

- `src-tauri/capabilities/dsh-page.json` — 补 `allow-read-file-base64` + 更新描述（P0）
- `src-tauri/src/commands.rs` — 删除两处 `/tmp` 调试日志（P1）
- `src-tauri/src/inject_js/js/right_sidebar/file_tree.js` — 删除 `console.log`（P2）
- `src-tauri/src/inject_js/mod.rs` — 移除脆弱花括号单测（P2）
- `.github/workflows/ci.yml` — 接入 `check-js.sh` JS 守门（P2）
- `README.md` — 补全 right_sidebar / file_link 文档（P1 文档漂移）
