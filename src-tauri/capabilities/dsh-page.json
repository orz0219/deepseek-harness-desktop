{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dsh-page",
  "description": "Grant the main launcher window IPC access to the custom commands from both the local setup/error page and the injected dsh web page (a remote loopback origin). The remote page may invoke the archive commands (scoped to dsh's own on-disk storage) plus the right-sidebar commands (git diff, file tree, file/image preview, clipboard, reveal-in-finder).",
  "windows": ["main"],
  "local": true,
  "remote": {
    "urls": ["http://127.0.0.1:*", "http://localhost:*"]
  },
  "permissions": [
    "core:default",
    "allow-get-status",
    "allow-select-dsh",
    "allow-restart-dsh",
    "allow-restore-archived-sessions",
    "allow-delete-archived-sessions",
    "allow-get-git-diff",
    "allow-get-file-tree",
    "allow-read-file-content",
    "allow-read-file-base64",
    "allow-copy-to-clipboard",
    "allow-reveal-in-finder"
  ]
}
